---
name: travel-headline-refiner
description: Refine, rewrite, or evaluate Chinese travel, scenery, geography, architecture, and destination headlines in the lively reference style. Use when a user asks to modify travel titles, make travel headlines more eye-catching or colloquial, match the style of 旅行类标题参考.csv, or provide several short headline alternatives for travel-lock-screen content.
---

# 旅行标题优化

将旅行内容标题改写为“好奇提问 + 反差/拟人化 + 具体奇观”的短标题。每次使用前先读 `references/style-guide.md`；涉及事实、地点、数据或“唯一/最”等绝对表述时，按下方核验流程处理，不把未确认信息写成确定事实。

## 先判断任务类型

先区分用户是在处理已有标题，还是在生成新的选题：

- **纯改写已有标题**：只优化用户提供标题的表达、节奏和风格，保留原意与事实边界；不新增地点、现象、数据或结论，不启动新选题流程。
- **根据素材拟标题 / 批量生成候选**：从素材提取标题信息，执行事实核验与候选差异化；素材缺少可确认奇点时，先向用户索取补充信息，不凭空编造。
- **审核标题**：检查标题的事实风险、疑问句标点和表达误导性；按“可用 / 需弱化 / 需核验”分类，并给出可直接替换的一版。

## 工作流程

1. 识别任务类型，提取地点、主角、可感知画面和真正的奇特点。
2. 从参考句式中选 1–3 种，优先写具体反差、异常现象或生活化联想；同批标题避免同一开头和同一句式重复。
3. 对涉及地点、现象、数据、历史或“唯一/最”等绝对表述的内容执行事实核验：优先查阅政府部门、景区/博物馆/自然保护区官方页面，以及科研机构、大学、专业数据库或权威媒体资料；搜索结果摘要只能作为线索，不能单独作为已验证依据。
4. 输出 5–10 个候选（用户指定数量时从其要求），每条单独一行。默认不加解释；如需，附 1 条“推荐：”并简短说明理由。
5. 完成最终自检：自然、短、能一眼看懂；不为吸睛制造虚假事实；不用恐慌、贬损、擦边或标题党式误导。

## 事实核验与标注

- 实际打开并读取到官方页面、文章正文或可靠资料，且来源支持标题中的关键事实，标记为 `[已验证]`。
- 只有搜索结果摘要、页面标题、用户转述或尚未打开核对的线索，标记为 `[待验证]`，不得写成已确认事实。
- 来源之间存在冲突，或无法确认“唯一、最大、最早、最”等绝对表述时，删除或弱化绝对词，并标记为 `[待验证]`。
- 找不到可核验源文章或页面时，如实写明“暂未找到可核验来源”，不得伪造、猜测或补造链接。
- 审核时说明触发核验的具体事实点，并给出弱化后的替换版本；纯改写没有新增事实时，不因语言润色虚构核验结果。

## 疑问句标点

- 只要标题使用“为什么、为何、咋、怎么、会不会、是否、也会、还能”等疑问或悬念结构，必须使用中文问号 `？`。
- 不使用问号时，改写为明确的陈述句，不保留疑问句语法。
- 正式输出使用中文全角标点；避免连续标点。

## 硬性要求

- 默认控制在 8–16 个汉字左右；需要适配锁屏时优先更短。
- 保留一个核心信息点，不把地点、背景、结论全部塞进标题。
- `？` 用于真实悬念或反常识；`！` 仅用于明确且可信的强奇观。
- 引号只突出一个具象拟人词、俗称或反差词，例如“巨眼”“变脸”“大冰箱”。
- 使用口语化中文，但避免网络烂梗、堆叠形容词、空泛“震撼/绝美/必去”。
- 不擅自使用“世界第一”“唯一”“最”“UFO”“外星人”“消失”等无法确认的表述；可改写成“像……”“传说中……”“疑似……”并按上方规则标注核验状态。

## 输出模式

- **改写已有标题**：保留原意，给出更像参考表的版本；不要改写成与素材无关的新选题。若未新增事实，可只输出标题结果。
- **根据素材拟标题**：先执行事实核验，再给多样候选；每个新选题附规范来源和核验状态。
- **审核标题**：标出“可用 / 需弱化 / 需核验”，说明具体问题，并给可直接替换的一版。

## 参考

完整规律、句式和已整理的样本见 [references/style-guide.md](references/style-guide.md)。
